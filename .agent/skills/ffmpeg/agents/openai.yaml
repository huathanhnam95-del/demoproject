interface:
  display_name: "FFmpeg Recipes"
  short_description: "Categorized FFmpeg commands for video automation."
  brand_color: "#5CB85C"
  default_prompt: "Use $ffmpeg to build the FFmpeg command for this task."

policy:
  allow_implicit_invocation: true
