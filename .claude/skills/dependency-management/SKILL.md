---
name: dependency-management
description: "Evaluate, add, update or remove a software dependency when the task needs that decision. Check existing capabilities, compatibility, maintenance, licensing, security and lockfile effects before changing the dependency graph."
---

# Shared workspace skill: dependency-management

Read the complete [workspace SKILL.md](../../../.agent/skills/dependency-management/SKILL.md) before applying this skill. It is the curated workflow for this workspace, with its complete supporting resources inside the same repository.

Resolve references and executable resources from `.agent/skills/dependency-management/`, not from this adapter directory. Preserve the current task scope, project rules and native host capabilities. Supporting files from the older Claude entry are retained for recovery but are not the current generic recipe.

Other workspaces use this account's full personal skill packages. If the workspace package is missing, report that limitation and use the matching personal skill when available; do not pretend the stale recipe is current.
