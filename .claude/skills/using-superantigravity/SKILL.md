---
name: using-superantigravity
description: "Resolve uncertainty about which installed skill or host workflow applies, especially when legacy Antigravity instructions conflict with the current host's capabilities. Use for skill routing questions; this is not an always-on startup or response-format rule."
---

# Shared workspace skill: using-superantigravity

Read the complete [workspace SKILL.md](../../../.agent/skills/using-superantigravity/SKILL.md) before applying this skill. It is the curated workflow for this workspace, with its complete supporting resources inside the same repository.

Resolve references and executable resources from `.agent/skills/using-superantigravity/`, not from this adapter directory. Preserve the current task scope, project rules and native host capabilities. Supporting files from the older Claude entry are retained for recovery but are not the current generic recipe.

Other workspaces use this account's full personal skill packages. If the workspace package is missing, report that limitation and use the matching personal skill when available; do not pretend the stale recipe is current.
