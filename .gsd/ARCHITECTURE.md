# Architecture

> Auto-generated by /map on 2026-01-20

## Overview

A hybrid web application for dictation and language practice. It features a rich Vanilla JS frontend that interacts with multiple backends: a Node.js server for external API wrapping (YouTube) and serving the app, a Python Flask server for advanced audio analysis (Praat), and a direct connection to Firebase Firestore for user progress persistence.

```
┌─────────────────────────────────────────────────────────────┐
│                      User Browser                           │
│  [index.html] [script.js (Logic)] [style.css (UI)]          │
└───────────────┬──────────────────────┬──────────────────────┘
                │                      │
       HTTP/API │             HTTPS/API│ (Direct)
                ▼                      ▼
┌───────────────────────┐      ┌──────────────────────────────┐
│    Node.js Server     │      │      Firebase Platform       │
│  [server.js] :8443    │◄────►│  [Firestore] [Auth]          │
│ - YouTube Transcripts │      └──────────────────────────────┘
│ - Static File Serve   │                     ▲
└───────────────────────┘                     │
                                              │
┌───────────────────────┐                     │
│     Python API        │                     │
│  [app.py] :5000       │◄────────────────────┘
│ - Audio Processing    │
│ - Praat Parselmouth   │
└───────────────────────┘
```

## Components

### Frontend Core

- **Location:** Root (`script.js`, `index.html`)
- **Purpose:** Handles all user interaction, game logic, state management, and UI rendering.
- **Key Logic:** `script.js` (>8000 lines) contains `progressCache`, `Dashboard` logic, `LearningMode` switches, and data loading from Excel files (`xlsx`).

### Backend - Node.js

- **Location:** `server.js`
- **Purpose:** Serves the application (SPA-like fallback) and provides API endpoints for data that requires server-side processing (e.g., YouTube transcripts).
- **Dependencies:** `express`, `youtube-transcript`.

### Backend - Python

- **Location:** `backend/`
- **Purpose:** Specialized audio analysis service using Praat.
- **Key Files:** `app.py` (Flask), `test_*.py` (Tests).
- **Dependencies:** `flask`, `praat-parselmouth`, `numpy`.

### Data Layer

- **Firebase:** Direct integration in frontend for Auth and Firestore (User progress, settings).
- **Static Data:** Excel files in `database/` (e.g., `WFD.xlsx`, `RS.xlsx`) used for question data source.

## Data Flow

1. **User Login:** Handled via Firebase Auth (or Guest mode).
2. **Data Loading:**
   - Frontend fetches `.xlsx` files from `database/` directory.
   - User progress fetched from Firestore (`window.firebaseFirestoreFunctions`).
3. **Practice Session:**
   - **Type/Speak Mode:** Logic handled in `script.js`.
   - **Video Mode:** `server.js` fetches YouTube captions.
   - **Audio Analysis:** Likely routed to Python backend (needs verifying if direct HTTP or via Firebase functions).

## Technical Debt

- [ ] **Monolithic Frontend:** `script.js` is over 8000 lines, mixing UI, state, and business logic.
- [ ] **Data coupling:** Hardcoded paths to Excel files (`database/type/WFD.xlsx`).
- [ ] **Multiple Servers:** Requires running both Node and Python servers locally for full functionality.
- [ ] **Global State:** Extensive use of `window` global variables (`window.DifficultyManager`, `window.firebaseFirestoreFunctions`).

## Conventions

**Naming:** camelCase for JS, snake_case for Python.
**Structure:** Flat structure for frontend, isolated `backend` folder.
**Testing:** Python backend has unit tests (`backend/test_*.py`), Frontend testing appears manual or non-existent in scan.
