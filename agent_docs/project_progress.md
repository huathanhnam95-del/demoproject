# Project Progress

## Locally verified package: V4FIX-UI-01 study-v2 review controls and visualization readiness

- Goal: make the production recording review workflow usable by repairing false WaveSurfer readiness failures and adding explicit automatic-version judgment plus precise timeline/A-B playback controls.
- Scope: the study-v2 CRM client/markup/styles, the smallest completion/export route contract, and focused route/browser tests in the clean release worktree `C:\Cursor AI-release-study-v2-20260823`. Preserve V4 automatic spans, manual-boundary promotion semantics, capture provenance, deterministic exposure, study-v1 retirement, and unrelated work.
- Constraints: Heavy route; test-first red/green implementation; exact deployed-source baseline `222038b0`; no commit, push, deployment, corpus mutation, claim/release, or production recording changes without a later explicit approval.
- Acceptance: real WaveSurfer 7 shadow-DOM waveform and delayed spectrogram surfaces become ready only after positive duration/dimensions; failures retain audio/analysis and expose retry; non-manual timeline clicks seek and play; A/B play-once and loop controls are precise and isolated from manual boundaries; any tied subset of V2/V3/V4 or exclusive None is required, validated, persisted, and exported; existing V3/V4 and study-v2 gates remain green.
- Ordered phases: exact-source diagnosis, test-first implementation, three reviewer repair loops, independent suites, and real-Chrome verification are complete. Git integration and production deployment remain separate user choices.
- Roles: root lead; Luna researcher completed read-only diagnosis; Luna coder implemented and repaired findings; Terra reviewer approved the final actual diff; Luna tester passed the corrected independent gates; Luna browser debugger passed a real WaveSurfer 7.12.6 Chrome check on its bounded retry.
- Root cause: the live waveform is visible but `visualizationReady` remains false because the client checks only `container.querySelector('canvas')`, while WaveSurfer 7 renders the waveform canvas inside Shadow DOM and the browser fixture incorrectly places it in light DOM.
- Verified result: the final six-file diff adds shadow-aware delayed readiness and retry, native-authoritative click-to-play and A/B stop/loop controls, manual-mode isolation, required tied/all/None automatic judgment, fail-closed route validation, immutable persistence/export, and explicit preservation of V4 `automaticSegments`. Fresh root route/browser/manifest/sync/lint/diff checks passed. Real Chrome rendered a `608x96` shadow-root waveform and `608x128` spectrogram with positive duration and zero console errors.
- Next action: choose local merge, push/PR, preserve the branch, or discard. Production remains unchanged and requires a separate explicit deployment approval.

## Completed package: V4FIX-ROLL-01 study-v2 controlled production rollout

- Goal: make the approved segmentation-study-v2 recording and assisted-review workflow usable in production from an exact, verified release SHA.
- Scope: the already-implemented study-v2 analyzer, Firebase Functions route and bundled manifest, idempotent v2 task seed, Hosting assets, and authenticated Chrome verification. Preserve the dirty primary checkout and all study-v1 data.
- Constraints: Heavy route; clean allowlisted release worktree; V3 isolation; genuine V4 schema/provenance; no phoneme-recognizer deployment; seed only missing v2 tasks; no recording or corpus deletion; record independent rollback targets.
- Acceptance: exact source/bundled/live manifest parity at 100 tasks; v2 dry-run shows 100 creates and zero v1 mutations before apply; live queue exposes 100 tasks; v1 mutations remain retired; waveform/spectrogram render without CSP errors; V2/V3/V4 identities are genuine; reversible claim/release leaves the queue clean.
- Ordered phases: release audit; clean-worktree verification; Cloud Run candidate and promotion; Functions deployment and v2 seed; Hosting deployment; authenticated Playwright and browser-debugger verification.
- Roles: root lead; Luna researcher for release audit; Luna coder for bounded release-worktree preparation and checks; Terra reviewer for exact diff/release scope; Luna tester for independent suites; Luna browser debugger for live Chrome evidence.
- Release evidence: application commits `7544c864`, `14c207d5`, pilot-selection commit `6a2dca8e`, holdout-lock commit `222038b0`, and release-record commit `251ccd30` are on `origin/main`. Cloud Run `praat-api-00066-ruy` serves 100% traffic from analyzer SHA `7544c864`; rollback is `praat-api-00064-coh`. Functions serves `api-00083-jev`. Hosting release `1787453015239000` uses version `00006ccff871ce31`.
- Data evidence: the immutable manifest is version `2.0.0`, SHA-256 `4db7d2c260fd5be5ac8e050f0cb31dfde2368cdba43453172bfeca4a352adfce`, and exposes exactly 100 study-v2 tasks. Seed apply created 100 tasks without changing the 100 study-v1 tasks; final queue state is 100 available, zero reserved, zero completed. Study-v1 remains readable and its mutations return `410 STUDY_RETIRED`.
- Runtime evidence: authenticated production Chrome loaded 100 queue items, verified the served `loadBlob` visualization path, proved a holdout UI click sends no claim, proved the API returns `409 HOLDOUT_LOCKED`, completed a reversible exact `abroad` claim/release, and returned to 100 available/zero reserved/zero completed with no relevant console errors. Public analyzer replay of the reviewed `photograph` label returned genuine complete V2/V3/V4 rows, three syllables each, partition schema `pronunciation-partition-variants-v2`, and V4 source `partitionVariants.v4`.
- Cohort protection: exact development tasks may be selected for the approved pilot; deterministic reservation records enforce one active task per operator/session. Explicit and ordinary claims both reject or skip all holdout tasks, including pre-existing holdout resumes. Holdout stays server-locked until a later separately authorized development-configuration freeze release.
- Verification evidence: the required Python, Node route, manifest/sync, browser fixture, lint, and release-contract suites passed independently; the final post-deploy gate passed 8/8 commands. The clean release worktree is retained at `C:\Cursor AI-release-study-v2-20260823`; production queue screenshot is under its ignored `test-results/segmentation-study-v2/` directory.
- Residual gates: the interactive browser-debugger workflow could not attach because its ChatGPT Chrome extension/native-host bridge was unavailable; two independent headless Chrome production passes succeeded. No promotion pass is permitted until the six historical source artifacts are restored. Begin only with the approved five-word development pilot before authorizing the remaining recordings.

## Active package: HPRON-01 pronunciation and notification recovery

- Goal: diagnose, fix, and verify the Pronounce-mode reference contour, HTTP 503, notification composite-index failure, and related Firestore Listen-channel errors shown on production.
- Scope: `public/pronunciation-analyzer/**`, the smallest relevant backend/service surface, `public/js/notification-center.js`, `firestore.indexes.json`, and focused logic/browser/index tests. Preserve all unrelated dirty work.
- Constraints: Heavy route; root-cause evidence before fixes; failing regression tests before production code; no push, deployment, production data mutation, or traffic change without separate explicit authorization.
- Acceptance status: lexical variant and measured-reference identity are aligned; the reference remains Merriam-Webster `photograph` noun variant `8050cda57bb448e7`. The collapsed duration canvas and offline SPA fallback are fixed locally. The notification query's exact index is declared and regression-tested, but is still absent from deployed Firestore. Chrome shows both local charts at `800x350` with zero console errors and 200 local WebChannel responses. Focused/broader checks and production source/API boot pass.
- Phases: HPRON-01A/B/C diagnosis and reproduction complete; HPRON-01D test-first implementation complete; HPRON-01E Terra actual-diff review and repairs complete; HPRON-01F local Chrome and command verification complete. Production deployment/verification remains pending explicit authorization.
- Roles: root lead; Luna researcher for pronunciation; Luna researcher for notification/index; Luna browser debugger; Luna coder after root-cause confirmation; Terra reviewer after implementation.
- Verified evidence: `npm run test:pronounce:logic`, Chrome pronunciation browser harness, service-worker config test, notification contract, exact Firestore-index contract, production source/API/CORS/schema boot, and scoped `git diff --check` all pass. Local 8081 health and 8082 readiness pass. Artifact: `test-results/hpron01f-ui/hpron01f-local-after-search.png`.
- Remaining gate: deployed indexes list only `uid + isRead + createdAt`; deploy the existing `uid + createdAt DESC` index and Hosting changes, then repeat production Chrome console/network verification. No push or deployment has occurred.
- Next action: obtain explicit production deployment authorization or leave the verified local patch for user-controlled release.

## Completed package: PRON-V3-LOCAL-ERROR-01

- Missing local recognizer configuration now propagates as `RECOGNIZER_CONFIG_MISSING` with safe diagnostics and non-consuming attempt-policy coverage.
- Unavailable V3 count/confidence render unavailable; degraded Praat spans remain explicitly display-only acoustic boundaries.
- The Windows local launcher starts a loopback-only phoneme module, waits on `/readyz`, rejects HTTP failure, bounds readiness waiting to approximately 180 seconds, and injects local disabled auth only after readiness; fallback clears recognizer variables and reports V3 unavailable.
- Fresh verification: 60 backend tests passed, pronunciation logic suite passed, launcher and attempt-policy contracts passed, exact `photograph` WAV replay returned V2 count 3 plus V3 `RECOGNIZER_CONFIG_MISSING` with null count, and Chrome screenshot evidence confirmed the corrected unavailable state.
- No production deployment, push, commit, or learner-facing V3 activation occurred.
