# Native Gemini integration decision — September 8, 2026

The USD 5 target is shared per staff UID and server Vietnam month across covered CRM AI features. It is monitored, not a provider-guaranteed maximum. Proportionate native testing on synthetic emulator records is authorized. No push, merge, deployment or production record mutation has occurred.

## Implemented local candidate

Native Gemini Live and Gemini 3.8 Flash are explicitly configured on the server. Exact captured PCM is separately transcribed by Flash; only completed server audio provenance can produce a final instruction or confirmation. Current staff and record authorization, exact preview binding, session freshness, atomic proof consumption and idempotent command receipts remain enforced.

Projects supports canonical previews for project creation, project metadata edits and task creation, plus existing task updates and moves. Creation Undo archives unchanged created records while retaining history. Current Owner authority is checked for metadata/creation Undo, including replay. Native Projects proposals use JSON mode with the exact schema in the system instruction and independent strict server validation; schema-mode requests returned generic HTTP 400 without identifying the rejected schema element. No automatic provider retry or output repair was added.

Checkpoints: 95ad3e17 native accounting/runtime; 9c9b1c9c resumption-frame validation and handle discard; 9b390267 canonical creation and Undo; fb9a5b45 creation proposals/UI and native JSON compatibility; c4719c7e bounded provider-frame batching; 6696b287 monitored usage UI refresh and Phase 3 refresh-test synchronization. Batching keeps the existing byte and job caps and rechecks current authority around normalization. The usage display distinguishes calculated usage, pending estimates and the remaining unreserved target.

## Actual native acceptance

Run native-edit-12 passed four assertions on September 8 at 03:35:48–03:36:43 UTC: spoken task instruction produced an exact visible preview without mutation; separate spoken confirmation persisted the task edit; page reload retained the same receipt without another apply; authenticated HTTP Undo restored the original title. This used actual Google Live and Flash, Chrome microphone/AudioWorklet and Auth/Firestore emulators with synthetic speech and records. Undo was not spoken. This run proves task editing; the later scenarios below extend that evidence.

The old credential returned free-tier HTTP 429 in native-create-project-13. That blocker is superseded: the new user-supplied key works through the restricted local Live configuration and shared resolver. Later project creation15, rename17, task creation18 and response mute19 passed, including separate spoken confirmation, persistence, reload and authenticated HTTP Undo. Rename17 exercised actual relay pause/resume without overflow. Mute19 stopped active AudioContext sources and suppressed later playback while processing and metering continued. No billing change or unlimited-quota claim follows from these successful calls.

## Local evidence and limits

Canonical Phase 9 passed 25 commands before the later JSON compatibility, Owner Undo replay and provider batching changes. Subsequent focused native/provider, creation, transport/identity and client tests passed. Canonical Phase 3 passed 3/3 on the refresh synchronization candidate; monitored usage client checks passed 34/34 and scoped lint passed. The full canonical run on 6696b287 completed 72/74 commands successfully, including performance. Both failures were the same stale shell cache-version expectation (directly and inside integration regression). Test-only checkpoint 4d221085 corrected three exact versions. Canonical Phase 0 then passed 9/9 and the nine-executable integration regression passed. All scheduled local checks are therefore covered across the full run and targeted correction reruns; this is not a single green all-phase run. Emulators were stopped by both runners. These local checks are distinct from the actual native scenario evidence summarized here.

The separate data-input checkout released d80841374737045504d857ef741696029bb1d4f7 with an opt-in native browser harness and exact English confirmation alongside Vietnamese. Both include current revision and payment wording; confirmation is recomputed on the server. Root independently ran 25 confirmation/UI tests and 20 atomic commit tests with the documented dependency path. Follow-up checkpoint 144ef102 removes persisted dispatch capabilities from both harness evidence writers; root inspected the diff and independently verified both actual redactors. Later data-input lead create a06, lead edit a02 and discard a01 passed on the documented separate candidates. Edit required one explicit typed name correction and exact record selection before automatic preview and speech confirmation. Canonical normalization also adds empty defaults to the minimal seed. Discard cleared bindings and produced no domain writes or paid reload. Data input supports discarding an unsaved draft, not post-save Undo; English testing does not establish Vietnamese ASR acceptance.

## Usage accounting

Final combined Projects/data-input run snapshots aggregate to USD 0.04564575 calculated from reported usage and USD 0.9346335 unresolved estimates. Separate preliminary probes add USD 0.00014775 and new-key validation adds USD 0.00002475 in known calculations, with additional unreported usage. These are not invoices or guaranteed bounds. Each integrated run used a fresh isolated emulator database; the aggregate is artifact bookkeeping, not one persistent test ledger.

Incomplete provider usage stays pending. Complete reported usage is priced against the pinned registry and may exceed its reservation or monthly target. Current admission includes an eight-unknown observation limit and blocks unresolved prior-month dispatches; these are application controls, not billing guarantees. Earlier hard-cost-proof blockers and Google billing-proof questions are superseded.
Current image acceptance: payment a03 passed on e3e427996e8e1b97da9dae78ba36bd26ec4b54f2 with one real Flash call, exact USD10.25/date2026-09-01, required received-funds acknowledgement, one payment/receipt and idempotent reload. Ambiguous-date a01 and adversarial/email a01 also passed on that exact source. The date was not invented and preview was rejected; adversarial instructions did not create unwanted actions and the exact email digest was preserved. Each image scenario used one Flash call and zero Live calls. Final independent checks passed 280/280 data-input JavaScript tests and 10/10 image Python tests. The two earlier payment harness setup failures made zero provider calls. Final exact-candidate integration gates remain with the coordinator; source worktrees are not merged.


Native scenario acceptance is complete; the coordinator retains combined-candidate integration, mounted UI/role audit, domain/feature-off regressions and shared staff/month accounting gates. Dedicated native runners/Auth are stopped. Runtime source remains e0561a34108ce1824df68f4429800b22ead9074b; this final update is documentation only. Sanitized evidence and exact handoff are in C:\Users\Admin\Documents\Codex\2026-09-08\crm-projects-recovery\outputs.

