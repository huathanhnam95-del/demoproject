# September 8 recovery execution contract

This resumes the already approved package in the preserved b699 checkout; it does not replace the roadmap or request renewed approval. Root owns integration and final audit. Phases 8, 9 and 10 remain open until their actual acceptance criteria are evidenced. Paid provider prerequisites may remain explicitly blocked with native dispatch disabled; independent local work continues.

Before snapshot: HEAD `30ffb9a3`, preceding source `4ba7cfd1`; tracked patch/status and untracked SHA256 inventory saved under `work/crm-projects-recovery/`. Existing dirty changes are preserved. Original task `01a078d3-733b-7fe1-b1fa-8f4980a738e0` is not loaded, latest turns completed; Phase8 audit records both prior workers released. Recovery agent inventory initially contains root only. Old heartbeat remains PAUSED.

Temporary user override: all assignments explicitly Astra Medium, isolated context, no nested agents. Fast is authorized but no supported active-tier setting/verification tool is exposed; do not claim it was applied. No global defaults changed.

Data-input task explicitly released canonical contract editing and shared budget integration to this recovery task. Canonical contract remains `docs/specs/shared-ai-assistance.md` here. Its checkout `C:/Cursor AI-data-input-20260907` remains externally owned/read-only. Its reserved ports are 8270/8271/9170/9370/9471/9472 and HTTP9270. Projects uses its isolated demo configuration and one coordinated emulator owner.

Accounting policy: ONE aggregate maximum USD5 per eligible staff UID/server-derived Vietnam calendar month across all covered CRM Live/Flash and supporting calls. Administrators may lower, never raise this ceiling. Domain authorization is independent of wallet availability. No native paid dispatch without proven enforceable bounds and separately authorized metered acceptance.

Current assignments: read-only accounting auditor owns no source; integration worker exclusively owns Projects router/access service, Firestore rules/indexes and focused API/cap tests. Root owns this recovery contract, canonical specification, phase audit and manifest integration. Additional ownership must be explicitly assigned after release. No push, merge, deployment, production mutation, secrets disclosure or paid calls.

Data-input consumer integration must exercise the actual `provider.generate({actorUid,feature:'crm-data-input',purpose:'draft',operationId,request})` interface, fresh domain authorization and the shared ledger. Its transport lifecycle API is not itself provider billing proof. Persisted last-balance races must cross real feature adapters, not merely arbitrary feature labels.
